import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.quintillizas.emi',
  appName: 'Quinstagram',
  webDir: 'dist/app',
  bundledWebRuntime: false
};

export default config;
